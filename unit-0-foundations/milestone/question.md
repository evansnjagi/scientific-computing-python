# Sensor Analysis

TODO:
    1. Come up with a list of ten float readings(i.e. temperature readings)
    2. Compute average
    3. Compute standard deviation
    4. Compute min-max
    5. Compute above/below average readings 
    6. Compute values that are within 1 standard deviation

Use `np.isclose()` somewhere, and write a short summary explaining what the module does, what each function measures, and connect everything to big O or scientific stack. 